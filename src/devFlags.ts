/**
 * ─────────────────────────────────────────────────────────────────────────────
 *  배포 전 삭제할 개발용 스위치
 * ─────────────────────────────────────────────────────────────────────────────
 *  실제 참여자 모집을 시작하기 전에 **이 파일을 삭제하고 참조를 모두 제거한다.**
 *  찾는 법: `grep -rn "devFlags\|STEP_BACK_NAV" src/`
 *
 *  `import.meta.env.DEV` 로 판단하지 않는 이유: 검증을 GitHub Pages 배포본
 *  (= 프로덕션 빌드)에서 하므로, DEV 게이트를 걸면 정작 확인해야 할 곳에서
 *  버튼이 사라진다. 그래서 "빌드에 포함되지만 한 곳에서 끄는" 방식을 쓴다.
 *  false 로 바꾸면 Vite 가 죽은 분기를 번들에서 제거한다.
 * ─────────────────────────────────────────────────────────────────────────────
 */

/**
 * 각 화면 하단에 "이전 단계로" 버튼을 띄운다.
 *
 * 없으면 3·4단계를 한 번 볼 때마다 동의부터 전부 다시 채워야 한다.
 *
 * ⚠️ 이 플래그가 켜져 있으면 뒤로 간 동의 화면이 **이미 체크된 상태**로 나타난다.
 *    미리 체크된 동의 체크박스는 실제 수집에서 허용되지 않는 형태다
 *    (동의는 참여자가 직접 체크하는 행위여야 한다). 삭제를 빠뜨리면 안 되는
 *    가장 중요한 이유가 이것이다.
 */
export const STEP_BACK_NAV = true;
