# 시스템 스펙 & 개발 로드맵

마지막 업데이트: 2026-09-09

> 이 문서는 새 프로젝트 폴더(`game-a11y-fundamental-test`)에 넣어두고, Claude Code 세션을 시작할 때 "이 문서 읽고 시작해줘"라고 요청하는 용도입니다.

---

## 1. 시스템 개요

이 프로젝트는 **완전히 분리된 두 개의 시스템**으로 구성된다. 참여자가 직접 쓰는 부분과, 관리자만 쓰는 부분은 저장소도, 배포 방식도, 개발 시점도 다르다.

### 시스템 A — 참여자용 웹페이지 (현재 개발 중, GitHub Pages 공개 배포)
참여자가 직접 접속해서 쓰는 유일한 부분. 이것만 지금 완성하면 1차 목표 달성.

0. **개인정보 수집 동의** (필수: 기본정보/민감정보/테스트데이터, 선택: 영상/논문활용) — 이 단계를 통과하지 못하면 이후 어떤 정보도 입력받지 않음
1. 참여자에게 고유 ID 발급
2. 참여자 기본정보 및 장애 양상 입력
3. 주로 플레이하는 게임 기기 및 보조기기 사양 입력
4. 선택한 장애 유형에 맞는 역량 테스트 수행 (유형별 최소 3~4개)
5. **결과를 CSV 파일로 다운로드** (파일명에 고유 ID 포함) → 참여자가 이 파일을 지정된 이메일로 제출하도록 안내

> 참여자는 여기서 끝난다. 리포트 생성, 영상 분석은 참여자가 보는 화면에 전혀 등장하지 않음.

### 시스템 B — 관리자용 리포트/분석 도구 (별도 프로젝트, 착수는 추후)
관리자(본인)만 쓰는 내부 도구. **완전히 다른 저장소/폴더에서 개발하며, 공개 배포하지 않는다.**

1. 이메일로 제출받은 CSV 파일들을 취합
2. 취합된 데이터를 바탕으로 AI가 게임 플레이 관점의 장애 양상 리포트 생성
3. 참여자(고유ID)의 게임 플레이 영상 업로드 + 위 리포트 연결 + 상황 서술 텍스트 입력
4. AI가 영상 + 리포트 + 서술을 바탕으로, 장면 단위로 문제와 정도를 수치화한 분석 리포트 생성
   - 예: "우측에서 나타난 적을 시야 범위 내 접근 전까지 인지하지 못해 반응이 O초 늦어짐"
5. (최종 목표) 이런 데이터가 축적되면 개발팀에 "시야각 조절 범위는 몇 도~몇 도로 제공해야 하는지" 같은 구체적 수치 인사이트 제공

> **왜 분리하는가**: 참여자는 기본정보 입력과 테스트만 하면 끝나고, 리포트 생성·영상 분석은 전부 관리자가 나중에 처리하는 일이기 때문. 또한 시스템 B는 여러 참여자의 민감정보가 집계된 형태로 다뤄지므로, GitHub Pages 같은 공개 호스팅에 올리면 안 되고 로컬 도구 또는 사내 전용 환경에서만 사용해야 한다.

---

## 2. 핵심 원칙: 접근성 (시스템 A 개발 전 기간 상시 적용)

이 프로젝트에서 접근성은 **두 층위로 나뉜다.** 이 구분을 혼동하면 안 됨.

### (1) 셸(Shell) — 전통적 웹접근성(WCAG) 완전 준수
네비게이션, 폼, 안내문, 버튼, 결과 화면 등 테스트 자극이 아닌 모든 화면.
- 키보드만으로 전체 완주 가능해야 함
- 스크린리더 호환 (semantic HTML, ARIA)
- 명확한 포커스 표시
- 시간제한이 있는 안내라면 대체 방식 제공

**저시력 사용자를 위한 시인성 기준 (스크린리더 못지않게 중요한 축)**
- **색 대비**: 본문 텍스트는 WCAG AA(4.5:1) 이상을 최소선으로 하되, 가능하면 AAA(7:1) 수준을 지향. 색상만으로 정보를 구분하지 말 것(아이콘·텍스트·패턴을 항상 함께 사용)
- **글자 크기**: 기본 본문 크기를 웹 표준 16px보다 크게 설정 (기본 18~20px 권장). px 고정값이 아닌 rem 단위를 써서 브라우저/OS 확대 설정에도 깨지지 않게 할 것
- **폰트**: 한글 시인성이 검증된 서체 사용 (예: Pretendard, Noto Sans KR 등). 얇은 굵기(Light/Thin)를 기본값으로 쓰지 말고 Regular~Medium 이상을 기본으로 사용. 자소가 밀집되지 않도록 자간·행간을 충분히 확보

### (2) 테스트 자극(Stimulus) — 표준화된 통제 조건 유지
반응속도, 대비 인식, 주변시야 인지 등 "측정하는 대상 그 자체"인 화면.
- 이 화면의 자극(색상 대비, 글자 크기, 애니메이션 속도, 타이밍)은 **유저의 브라우저/OS 접근성 설정에 의해 임의로 바뀌면 안 됨** — 측정값의 유효성이 깨지기 때문
- 단, 자극이 아닌 **안내 문구·버튼 등 주변 UI**는 위 셸 기준(색 대비, 글자 크기, 폰트)을 그대로 따름 — 통제되어야 하는 건 "측정 대상 자극"뿐이지 화면 전체가 아님
- 대신 **입력 방식**은 유연해야 함: 온보딩에서 입력받은 보조기기 정보를 바탕으로, 각 테스트가 요구하는 입력 방식(마우스/키보드/스위치 등)을 명시하고, 맞지 않으면 대체 테스트를 제공하거나 해당 테스트를 건너뛰는 로직 필요

---

## 3. 참여자 정보 입력 설계 (시스템 A)

### 3-1. 고유 ID 발급 규칙 (확정)

**형식**: `GA11Y-[경로코드][연도 2자리]-[일련번호 3자리]`

| 구성요소 | 설명 | 예시 |
|---|---|---|
| GA11Y | 프로젝트 고정 접두어 | GA11Y |
| 경로코드 (1자리) | P = 접근성 패널 프로그램 / A = 스마일 플레이 앨라이 프로그램 / O = 기타 | P |
| 연도 (2자리) | 등록 연도 | 26 (2026년) |
| 일련번호 (3자리) | 해당 연도·경로 내 등록 순번, 001부터 (매년 1월 리셋) | 014 |

예시: `GA11Y-P26-014`
검증 정규식: `^GA11Y-[PAO]\d{2}-\d{3}$`

**중요 — ID에 담을 정보의 범위를 제한할 것.** 경로·시기·순번처럼 비민감 관리정보만 담고, 나이·성별·장애유형 같은 민감정보는 절대 ID 안에 넣지 않는다. ID는 관리자 화면, CSV 파일명, 리포트 제목 등 여러 곳에 노출되므로, 민감정보를 ID에 담으면 그 자체로 새로운 노출 경로가 생긴다.

**현재 한계**: 이 검증은 "형식이 맞는지"만 확인하며, 그 ID가 실제로 관리자가 발급한 것인지는 확인하지 않는다. 발급 목록과 대조하는 검증은 시스템 B(관리자 도구) 쪽 과제로 고려.

### 3-2. 기본정보 페이지 구성

1. **고유 ID** (단일 텍스트 입력창, 분할 입력박스 방식 사용 금지)
   - 라벨 아래 항상 보이는 예시: "예: GA11Y-P26-014"
   - 입력값을 실시간으로 정규식(`^GA11Y-[PAO]\d{2}-\d{3}$`) 검증 — 맞으면 초록 체크, 틀리면 빨간 테두리 + "형식이 올바르지 않습니다. 예: GA11Y-P26-014" 문구
   - 소문자로 입력해도 자동으로 대문자 변환 (관대한 입력 처리)
   - **분할 입력박스(한 칸에 한 글자) 방식은 쓰지 않는다** — 스크린리더 낭독 흐름이 끊기고, 스위치·아이트래커 사용자는 칸마다 초점 이동이 늘어나 조작 비용이 커짐. 접근성 우선순위와 상충
2. **성별** — 남성 / 여성 / 직접 입력(선택 시 자유 텍스트란 노출) / 밝히고 싶지 않음
   - 이분법에 포함되지 않는 정체성을 하나의 "기타"로 뭉뚱그리지 않고, 스스로 표현할 수 있게 자유 입력을 제공. 원치 않으면 비워도 되고, "밝히고 싶지 않음"을 선택하면 자유 입력란은 노출하지 않음
3. 연령대 — 14~19세 / 20대 / 30대 / 40대 / 50대 / 60대 이상 (동의서의 만 14세 기준선과 일치시킴, 세분화하지 않는 이유는 장애유형·기기 조합과 겹쳐 개인 역추적 위험을 낮추기 위함)
4. 참여 경로 — 접근성 패널 프로그램 / 스마일 플레이 앨라이 프로그램 / 기타(선택 시 직접입력란 노출)
5. 게임 경력·숙련도 (자기평가, **라디오**) — 입문 / 초급 / 중급 / 상급
6. 선호 게임 장르 (다중선택 체크박스) — FPS, TPS, RPG, 스포츠, 리듬게임, 퍼즐, 전략/시뮬레이션, 기타(직접입력)
7. 주간 평균 게임 플레이 시간 (**라디오**) — 5시간 미만 / 5~10시간 / 10~20시간 / 20시간 이상
8. **평소 쓰는 보조기기 사용 여부** — 예 / 아니요 (라디오)
   - 여기는 **일상생활이나 컴퓨터를 쓸 때 늘 사용하는** 보조기기를 받는다. 게임할 때 쓰는 보조기기는 3-4에서 따로 받는다
   - "예" 선택 시 "명칭 또는 모델명" 자유 입력란 노출 (예: 스크린리더(NVDA·센스리더), 화면 확대 프로그램, 보청기·인공와우, 휠체어, 의사소통 보조기기, 모르면 비워도 됨)
   - 힌트에 "게임할 때만 쓰는 기기는 다음 단계에서 따로 물어봅니다"를 명시한다. 이 구분이 없으면 NVDA 처럼 일상에도 게임에도 쓰는 기기를 어디에 답해야 할지 참여자가 알 수 없다
9. 평소 게임 내 접근성 기능 사용 여부 (다중선택 체크박스) — 자막, 색맹모드, 버튼 리매핑, 난이도 조절, 사용 안 함

> **드롭다운 → 라디오 변경 (구현 시 결정)**: 5·7번을 드롭다운으로 적었으나 라디오로 구현했다.
> 1번에서 분할 입력박스를 거부한 근거("스위치·아이트래커 사용자는 칸마다 초점 이동이 늘어나
> 조작 비용이 커짐")가 드롭다운에도 그대로 적용된다 — 열기→탐색→선택 3단계가 필요하다.
> 또 OS 가 그리는 select 옵션 목록은 이 프로젝트의 24px 글자 크기를 따르지 않아 작게 보인다.
> 선택지가 4개뿐이라 한 화면에 모두 펼쳐 보이는 편이 낫다.

### 3-3. 장애 정보 입력 (3단계 구조)

**1단계 — 법정 장애 유형 및 장애 정도 선택** (장애인복지법 시행령 기준 15개 유형, 다중선택 가능)
안내문: "게임 플레이에 영향이 있다고 생각되는 장애 유형을 선택해주세요."

화면 복잡도를 낮추기 위해, 게임 플레이 접근성과 직접 연관성이 높은 유형만 기본 노출하고 나머지는 "+ 그 외 유형 보기"로 접어둔다. 완전히 삭제하는 것이 아니라 노출 우선순위만 조정 — 어떤 유형을 선택하든 2단계 세부양상 질문은 동일하게 작동한다.

| 노출 방식 | 유형 |
|---|---|
| 기본 노출 (7개) | 시각장애, 청각장애, 지체장애, 뇌병변장애, 지적장애, 자폐성장애, 뇌전증장애 |
| "+ 그 외 유형 보기" (8개, 접힘) | 언어장애, 안면장애, 신장장애, 심장장애, 간장애, 호흡기장애, 장루·요루장애, 정신장애 |

> 뇌전증장애는 법적 분류상 "신체 내부 기관장애"에 속하지만, 점멸광에 의한 발작 위험 등 게임 접근성과 직결되므로 예외적으로 기본 노출 그룹에 포함시켰다.

**장애 정도(중증/경증) 입력**
유형을 체크하면 바로 옆에 정도를 묻는 라디오가 인라인으로 나타남: **중증 / 경증**
- 안내문: "복지카드에 표시된 장애 정도를 참고해주세요."
- 법적 근거: 2019년 장애등급제(1~6급) 폐지 이후, 장애인복지법 시행규칙상 '장애의 정도가 심한 장애인(중증)' / '심하지 않은 장애인(경증)' 2단계로 구분. 복지카드에도 이 명칭으로 표기됨
- 여러 유형을 선택한 경우, 유형별로 정도가 다를 수 있으므로 유형마다 각각 입력받음

**2단계 — 선택한 유형별 게임 관련 세부 양상** (체크박스, 선택한 유형에 대해서만 순차적으로 노출)

유형별이 아니라 **양상 그룹별**로 묶어 노출한다. 지체장애와 뇌병변장애는 게임 조작에서 물어볼 것이 같으므로, 둘을 모두 선택한 사람에게 같은 질문을 두 번 하지 않는다.

| 그룹 | 해당 유형 |
|---|---|
| 시각 | 시각장애 |
| 청각 | 청각장애 |
| 조작 | 지체장애, 뇌병변장애 |
| 인지·감각 | 지적장애, 자폐성장애, 정신장애 |
| 뇌전증 | 뇌전증장애 |
| 체력·지속 시간 | 신장·심장·간·호흡기·장루요루장애 |
| 말하기 | 언어장애, 안면장애 |

**문구 작성 기준 (구현 시 확정)**

1. **게임 조작에서 실제로 갈리는 것만 남긴다.** "근력저하" 같은 묶음 표현은 테스트 설계에 쓸 수 없다 — 버튼을 오래 누르기 어려운 것과 빠르게 연타하기 어려운 것은 서로 다른 대안이 필요하다.
2. **진단명이 아니라 참여자가 겪는 일로 적는다.** "안구진탕" 대신 "화면의 한 점을 계속 보기 어려움". 참여자는 의료인이 아니고, 진단명을 모르는 사람이 자기 상태를 못 고르면 데이터가 비뚤어진다. 의학 용어는 괄호로 덧붙여 아는 사람이 확인할 수 있게만 둔다.
3. **좌우 구분이 필요한 항목은 선택 시 인라인 라디오(오른쪽/왼쪽)로 받는다.** 항목을 좌우로 쪼개 나열하면 목록이 두 배가 된다. 버튼 리매핑·대안 테스트 설계에 쓰이는 정보다.

항목 전문은 `src/data/disabilityFields.ts` 참고. 예시:

- 시각 → 전맹 / 교정해도 흐리게 보임 / 작은 글자 / 색 구분 / 밝기 차이 / **빛 번짐** / **한쪽 시야 결손(좌우)** / 양쪽 주변 시야 결손 / 중심 시야 결손 / 시야 흔들림 / 기타
- 조작 → **한쪽 손·팔(좌우)** / 양쪽 손·팔 / 손 떨림 / 손가락 힘 / 팔 힘 / **연타** / 동시 입력 / 정밀 조준 / 불수의 운동 / 다리·발 / 지속 피로 / 기타
- 인지·감각 → 반응 속도 / **갑자기 나타나는 화면 신호(QTE)** / 제한 시간 압박 / 복잡한 지시 / 정보 과다 / 집중 지속 / 기억 / 감각 과민 / 기타

**검증**: 노출된 그룹마다 1개 이상 필수(각 그룹에 "기타 직접 입력"이 있어 막히지 않음). 유형을 해제하면 그 유형의 정도와, 더 이상 필요 없어진 그룹의 양상·좌우 값을 함께 버린다.

**3단계 — 주관식 부연 설명** (선택, 권장)
안내문: "본인의 장애 양상이 게임 플레이에 전반적으로 어떤 영향을 미치는지 설명해주세요. 특정 상황을 서술하기보다는, 평소 어떤 어려움이 있는지 위주로 적어주시면 좋습니다."

예시 문장 (안내문 아래 항상 노출):
- "저시력이라 화면 가장자리에 있는 사물은 잘 인지하지 못하는 편입니다."
- "손 떨림이 있어서 정밀한 조준이 필요한 상황에서 반응이 늦어지는 경우가 많습니다."
- "소리로 오는 신호(발소리, 경고음)를 알아차리기 어려워서 시각적 신호에 더 의존합니다."

### 3-4. 기기 사양 페이지 (PC 기본 + 기기 추가 가능)

각 항목은 정해진 값이 있으면 드롭다운을 우선하고, 예외 상황만 "기타(직접입력)"으로 받는다. 모든 확인방법 설명은 호버 툴팁이 아니라 **항상 보이는 캡션 또는 키보드로 열리는 접기/펼치기**로 제공 (스크린리더·키보드 전용 사용자도 동일하게 접근 가능해야 함).

| 항목 | 입력 방식 | 확인 방법 안내 (화면에 상시 노출) | 예시 |
|---|---|---|---|
| 운영체제 | 드롭다운 | — | Windows 10, Windows 11, macOS, Linux, 기타 |
| 모니터 크기 | 드롭다운 | "모니터 뒷면 스티커나 구매 영수증에서 확인 가능. 정확히 모르면 대략적인 크기를 선택해주세요." | 21~24인치, 27인치, 32인치 이상, 기타 |
| 해상도 | 드롭다운 + 기타 직접입력 | "확인 방법: 바탕화면에서 마우스 우클릭 → 디스플레이 설정 → '디스플레이 해상도' 항목에서 확인 (예: 1920x1080)" | 1920×1080(FHD), 2560×1440(QHD), 3840×2160(4K), 기타 |
| 주사율 | 드롭다운 | "확인 방법: 디스플레이 설정 → 고급 디스플레이 설정 → '화면 주사율' 항목에서 확인. 단위는 Hz(헤르츠)입니다." | 60Hz, 144Hz, 240Hz, 기타 |
| 주 입력장치 | 다중선택 체크박스 | — | 마우스, 키보드, 게임패드(컨트롤러), 스위치, 아이트래커, 기타 |
| 화면과의 평균 시청 거리 | 드롭다운 | "자로 재지 않으셔도 됩니다. 팔을 하나 뻗은 정도(약 50~70cm)를 기준으로 가늠해주세요." | 50cm 미만, 50~100cm, 100cm 초과 |
| 오디오 출력 방식 | 드롭다운 | — | 스피커, 유선 이어폰·헤드셋, 무선 이어폰·헤드셋 |

**게임할 때 쓰는 보조기기** (항상 노출, 라디오 3택 · 필수)

3-2의 "평소 보조기기"와 구분된다. 3-2는 일상·범용, 여기는 **게임 플레이에서 활용하는** 보조기기다.

| 선택지 | 동작 |
|---|---|
| 게임할 때 쓰는 보조기기가 따로 있습니다 | 명칭/모델명 자유 입력 노출 (필수). 예: Xbox 적응형 컨트롤러, 한손 컨트롤러, 스위치 인터페이스, 아이트래커, 헤드 마우스, 발 페달, 커스텀 키패드 |
| 평소 쓰는 보조기기를 게임할 때도 그대로 씁니다 | 3-2 값을 읽기 전용으로 이어받아 표시. 같은 질문을 두 번 하지 않는다. **3-2에서 "아니요"면 이 선택지를 숨긴다** |
| 게임할 때는 보조기기를 쓰지 않습니다 | — |

> **구현 시 변경**: 원래 "3-2에서 예를 고른 경우에만 이 섹션 노출"이었으나 **항상 노출**로 바꿨다. 일상 보조기기는 없지만 게임용 적응형 컨트롤러를 쓰는 참여자가 실제로 있고, 조건부로 두면 그 데이터가 유실된다. 대신 "평소 쓰는 것을 그대로 씁니다" 선택지로 중복 질문을 피한다.

**그 외 게임에 쓰는 기기** (선택, 여러 대 추가 가능)
- **모델명만** 받는다. "PlayStation 5", "Nintendo Switch OLED" 처럼 모델명에 기기 종류가 이미 담긴다
- "기기 추가" / "부가 기기 N 삭제" 버튼. 삭제 버튼 이름에 번호를 넣어 여러 개일 때 구분되게 한다
- 항목마다 고정된 id 를 붙인다. 배열 인덱스를 키로 쓰면 중간 항목 삭제 시 뒤 항목의 DOM id 가 밀려 label 연결과 포커스 대상이 어긋난다
- 추가 시 새 입력란으로 포커스 이동(사용자가 직접 누른 동작이므로 기대되는 이동), 삭제 시 "기기 추가" 버튼으로 포커스 이동(포커스가 사라져 body 로 튕기는 것을 막는다)

**입력 방식**: 표의 드롭다운 항목은 모두 라디오로 구현했다. 근거는 3-2의 동일 결정 참고.

**"모르겠음" 선택지**: 해상도·주사율·화면 크기·운영체제에 추가했다. 모르는 사람에게 추측을 강요하면 틀린 값이 들어와 측정 해석을 망친다. 모른다는 사실 자체가 유효한 데이터다.

**오디오 "소리를 쓰지 않음"**: 표의 3택(스피커/유선/무선)만 두면 청각장애 참여자가 거짓 응답을 하게 되므로 추가했다.

**화면 크기 "노트북 내장 화면"**: 외장 모니터 인치 구간만 나열하면 노트북 사용자가 고를 항목이 없으므로 추가했다.

시각 테스트(글자크기·대비인식)의 유효성은 시청 거리에 직접 영향을 받으므로 이 항목은 필수로 받는다. 오디오 출력 방식은 무선 기기의 지연(레이턴시)이 반응속도 테스트 해석에 영향을 주므로 포함한다.

### 3-5. 결과 제출 방식 (구글시트/서버 대신 CSV)

- 모든 테스트 완료 시, 결과를 **CSV 파일로 즉시 다운로드**
- 파일명 형식: `{고유ID}_{제출일자 YYYYMMDD}_결과.csv` (예: `GA11Y-P26-014_20260909_결과.csv`)
- 다운로드 직후 화면에 안내 문구 표시: "다운로드된 파일을 아래 이메일로 보내주세요: [제출용 이메일 주소]"
- 서버/구글시트 저장은 사내 보안 검토가 끝난 뒤 시스템 B 단계에서 재검토

---

## 4. 장애유형별 테스트 설계 (예시: 시각장애) — 시스템 A

최소 3~4개 테스트로 게임 플레이에 필요한 세부 역량 데이터 수집.

1. 기본 반응속도
2. 대비 변화에 따른 객체 인식 역량
3. 중심 응시 중 주변에서 다가오는 객체 인지 속도 (주변시야)
4. 글자 크기 인식 최소 임계값
5. 동일 글자크기의 대비별 인식 차이

> 다른 장애 유형(청각, 운동, 인지 등)은 시각장애 테스트에서 잡힌 구조를 템플릿으로 삼아 동일한 패턴으로 설계. 3-3의 "게임 관련 세부 양상"과 대응되게 설계할 것.

---

## 5. AI 리포트 생성 원칙 — 시스템 B (별도 프로젝트, 착수는 추후)

- **파인튜닝보다 먼저 프롬프트 엔지니어링으로 시도할 것.** 구조화된 수치를 프롬프트에 정확히 전달하고, 판단 기준표(rubric)를 함께 제공해서 할루시네이션 억제
- 자유 서술형 추측 최소화 — 수치를 벗어난 판단을 하지 않도록 명시
- 이 방식으로도 품질이 부족할 때만 파인튜닝 등 추가 학습 고려
- 영상 분석 리포트는 반드시 정량 리포트(CSV 기반)를 함께 입력으로 제공 — 영상만으로 판단하지 않도록
- 이 도구는 공개 배포하지 않는다 (여러 참여자의 민감정보가 집계된 형태로 다뤄지므로)

---

## 6. 개발 로드맵

### 시스템 A — 참여자용 웹페이지 (현재 진행, GitHub Pages 공개 배포)

| 단계 | 내용 |
|---|---|
| Phase 0 | 폴더 구조, 접근성 원칙 문서화 |
| Phase 1 | 동의 화면(필수/선택 항목 구분, 미동의 시 진행 불가) → 기본정보 페이지 → 장애정보 페이지 → 기기사양 페이지 — 접근성 검증 포함 |
| Phase 2 | 테스트 템플릿 1개 완성 (예: 반응속도) — 정밀 측정, 입력방식 명시 |
| Phase 3 | 템플릿 복제 → 시각장애 나머지 테스트 3~4개 |
| Phase 4 | 다른 장애유형에 동일 패턴 반복 |
| Phase 5 | 테스트 종료 화면 — 결과 CSV 다운로드(고유ID 포함 파일명) + 이메일 제출 안내 |

### 시스템 B — 관리자용 리포트/분석 도구 (별도 프로젝트, 착수는 추후)

| 단계 | 내용 |
|---|---|
| Phase B0 | 이메일로 받은 CSV 파일들을 취합하는 대시보드 |
| Phase B1 | AI 리포트 생성 기능 |
| Phase B2 | 영상 업로드 + AI 분석 기능 |

> 시스템 B는 완전히 다른 저장소/폴더에서 시작하며, 공개 배포하지 않는다.

---

## 7. Claude Code 작업 원칙

1. **기능 하나 = 요청 하나.** 여러 기능을 한 프롬프트에 묶지 않기
2. **코드 요청 전 "계획부터 설명해줘"** 로 설계 오류를 코드 짜기 전에 걸러내기
3. **기능 완성 후 별도로 "접근성 관점에서 점검해줘, 특히 키보드만으로도 되는지, 색 대비와 글자 크기가 저시력 기준을 충족하는지"** 요청하기
4. Phase 2(첫 테스트 템플릿)는 이후 모든 테스트의 기반이 되므로 가장 신경 써서 검증
5. 시스템 A 작업 중에는 시스템 B(리포트 생성, 영상 분석) 관련 코드를 절대 같이 요청하지 않기 — 완전히 별도 프로젝트로 취급
